import { ProOptions } from '@xyflow/react';

export * from './widgets';

export * from './widget-panel-config';

export const proOptions: ProOptions = {
  account: 'paid-pro',
  hideAttribution: true,
};
